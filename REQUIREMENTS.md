# 智能家居网关 v2.0 — 需求规格说明书

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 智能家居网关模拟器 v2.0 |
| 版本 | 2.0.0 |
| 作者 | 曹彬 |
| 日期 | 2026-07-30 |
| 状态 | 开发中 |

---

## 1. 项目背景

### 1.1 业务背景

智能家居场景中，家庭内部部署多种环境传感器（温度、湿度、光照、气压、燃气），数据通过网关汇总后实时推送给终端用户（手机 App、Web 控制台）。网关是家庭 IoT 网络的**数据枢纽**。

### 1.2 技术背景

v1.0 版本（已完成）实现了基础的数据采集→推送链路：5 个传感器线程模拟数据生成，经事件队列分发后由 epoll TCP 服务器广播给所有客户端。功能可用但存在以下局限：

| v1.0 问题 | 面试暴露的风险 |
|-----------|---------------|
| 纯文本协议（emoji+明文） | 被问"为什么不用 JSON/protobuf"时会尴尬 |
| 全量广播，客户端无法选择 | 体现不出权限/过滤设计思维 |
| 无心跳检测，断线感知靠 TCP 超时 | 被问"怎么感知客户端断开"时答案被动 |
| 820 行，体量单薄 | 与 5700 行音箱项目差距过大，简历不均衡 |
| 无内核交互 | 缺少"系统编程深度"标签 |

### 1.3 v2.0 目标

在不引入第三方库的前提下，通过以下升级把网关打造成 **应用层+内核层交叉的示范项目**：

1. **协议升级**：文本 → JSON，自研极简解析器
2. **订阅机制**：位掩码过滤，客户端按需接收数据
3. **心跳检测**：应用层 PING/PONG，精确感知断线
4. **系统监控**：通过 /proc 文件系统 + netlink 采集系统运行状态，作为"系统传感器"推送给客户端
5. **内核模块**：最小内核模块通过 /proc 暴露数据，演示用户态↔内核态通信

---

## 2. 功能需求

### FR-01：传感器数据采集

| 编号 | FR-01 |
|------|-------|
| 描述 | 多线程模拟传感器数据采集 |
| 优先级 | P0（继承自 v1.0） |
| 详细 | 每个传感器一个独立 pthread，按配置文件中的间隔采集数据，写入线程安全事件队列 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-01.01 | 支持 5 种传感器类型：温度、湿度、光照、气压、燃气 |
| FR-01.02 | 传感器参数（名称、范围、间隔）从 `gateway.conf` 配置文件读取 |
| FR-01.03 | 温度用整型 ×10 表示（避免浮点精度问题），范围 22.0–38.0°C → 220–380 |
| FR-01.04 | 收到 SIGINT 后所有传感器线程优雅退出（通过 g_stop 标志 + pthread_join） |

---

### FR-02：事件分发

| 编号 | FR-02 |
|------|-------|
| 描述 | 事件队列 + 解耦分发器 |
| 优先级 | P0（继承自 v1.0） |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-02.01 | 环形缓冲区实现的线程安全事件队列（容量 128），使用互斥锁 + 条件变量 |
| FR-02.02 | 表驱动分发器：每个事件类型注册一个回调函数（event_handler），遍历匹配表调用 |
| FR-02.03 | 支持运行时注册新事件类型（注册表扩容上限 16 个） |

---

### FR-03：epoll TCP 服务器

| 编号 | FR-03 |
|------|-------|
| 描述 | 基于 epoll 的非阻塞 TCP 服务器，管理多客户端连接 |
| 优先级 | P0（继承自 v1.0） |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-03.01 | 使用 epoll_create1 + epoll_wait 实现非阻塞事件循环 |
| FR-03.02 | 支持最大 64 个并发客户端 |
| FR-03.03 | EPOLLET 边缘触发模式 |
| FR-03.04 | 新客户端连接后自动推送全量最新传感器快照 |
| FR-03.05 | 服务端端口从配置文件读取 |

---

### FR-04：JSON 协议（v2.0 新增）

| 编号 | FR-04 |
|------|-------|
| 描述 | 使用 JSON 格式进行服务器↔客户端通信 |
| 优先级 | P0 |
| 引入原因 | 结构化协议是实际项目的标准做法，同时体现协议设计能力 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-04.01 | 服务器推送使用统一 JSON 格式：`{"event":"类型","value":值,"unit":"单位","time":时间戳}` |
| FR-04.02 | 客户端命令使用 JSON 格式：`{"cmd":"命令名","type":["列表"]}` |
| FR-04.03 | JSON 解析器零依赖、手写实现，不超过 300 行 C 代码 |
| FR-04.04 | 解析器需处理：缺失字段、格式错误、缓冲区溢出，返回明确错误码 |
| FR-04.05 | 序列化函数需处理浮点格式化（精确到 1 位小数）和特殊字符转义 |

**命令集合：**

| 命令 | 格式 | 响应 |
|------|------|------|
| SUB | `{"cmd":"SUB","type":["temp","humi"]}` | 无响应（静默更新） |
| PING | `{"cmd":"PING"}` | `{"result":"PONG"}` |
| LIST | `{"cmd":"LIST"}` | `{"result":"LIST","data":[...]}` |
| CLIENTS | `{"cmd":"CLIENTS"}` | `{"result":"CLIENTS","count":N}` |

---

### FR-05：订阅过滤（v2.0 新增）

| 编号 | FR-05 |
|------|-------|
| 描述 | 每个客户端可按需订阅传感器类型 |
| 优先级 | P0 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-05.01 | 用 uint32_t 位掩码表示订阅集合（5 个传感器位 + 1 个系统信息位） |
| FR-05.02 | 新客户端默认订阅全部（SUB_ALL），零配置可用 |
| FR-05.03 | 客户端发送 `SUB` 命令更新掩码，空数组取消所有订阅 |
| FR-05.04 | 广播时使用 `(client->sub_mask >> event_bit) & 1` 判断是否推送（O(1)） |

---

### FR-06：心跳检测（v2.0 新增）

| 编号 | FR-06 |
|------|-------|
| 描述 | 应用层心跳检测，感知客户端断线 |
| 优先级 | P1 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-06.01 | 客户端每 10 秒发送 `{"cmd":"PING"}`，服务器回复 `{"result":"PONG"}` |
| FR-06.02 | 服务器在 epoll_wait 每次返回时检查各客户端 `last_heartbeat`，超时 30 秒则关闭连接 |
| FR-06.03 | 心跳检测不占用额外线程，集成在 epoll 事件循环中 |

---

### FR-07：系统监控（v2.0 新增）

| 编号 | FR-07 |
|------|-------|
| 描述 | 采集网关所在设备的 CPU/内存/进程数，作为"系统传感器"推送给客户端 |
| 优先级 | P1 |
| 技术方案 | /proc/stat 解析 CPU + /proc/meminfo 解析内存 + /proc/loadavg 负载 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-07.01 | 每 5 秒采集一次系统信息（可通过配置文件调整） |
| FR-07.02 | CPU 使用率用 **delta 算法**（两次采样差值 / 总时间差），精确到 0.1% |
| FR-07.03 | 系统信息以统一 JSON 格式推送：`{"event":"sysinfo","cpu_pct":23.5,...}` |
| FR-07.04 | 作为独立传感器线程实现，复用现有传感器框架 |

---

### FR-08：内核模块（v2.0 新增）

| 编号 | FR-08 |
|------|-------|
| 描述 | 编写最小 Linux 内核模块，在 /proc/gateway/info 暴露内核态数据 |
| 优先级 | P2（加分项，在 QEMU 上运行） |
| 开发环境 | 在 Buildroot/QEMU ARM64 环境中编译运行 |

**子需求：**

| 编号 | 描述 |
|------|------|
| FR-08.01 | 模块加载时创建 `/proc/gateway/info` 文件节点 |
| FR-08.02 | 读取该文件返回：系统进程总数、空闲内存页数 |
| FR-08.03 | 模块卸载时删除 /proc 节点 |
| FR-08.04 | 网关用户态程序读取 `/proc/gateway/info`，将数据整合到系统信息推送中 |
| FR-08.05 | 提供独立 Makefile，可在 QEMU Buildroot 环境中 `make` 编译 |

---

### FR-09：配置文件驱动

| 编号 | FR-09 |
|------|-------|
| 描述 | 运行时参数通过配置文件配置 |
| 优先级 | P0（继承自 v1.0，扩展字段） |

**配置项：**

```ini
# gateway.conf
server.port = 8888
server.max_clients = 64

# 心跳超时秒数（v2.0 新增）
server.heartbeat_timeout = 30

# 系统信息采集间隔毫秒（v2.0 新增）
sysmon.interval_ms = 5000

sensor[0].name = TempSensor
sensor[0].min = 220
sensor[0].max = 380
sensor[0].interval = 2000

sensor[1].name = HumiSensor
sensor[1].min = 30
sensor[1].max = 90
sensor[1].interval = 3000

sensor[2].name = LightSensor
sensor[2].min = 50
sensor[2].max = 800
sensor[2].interval = 5000

sensor[3].name = PressureSensor
sensor[3].min = 980
sensor[3].max = 1050
sensor[3].interval = 4000

sensor[4].name = GasSensor
sensor[4].min = 0
sensor[4].max = 100
sensor[4].interval = 5000
```

---

## 3. 非功能需求

### NFR-01：性能

| 指标 | 目标值 | 测量方式 |
|------|--------|---------|
| 传感器到推送端到端延迟 | < 50ms | 时间戳差值 |
| epoll_wait 事件处理延迟 | < 1ms | 循环内打点 |
| 支持并发客户端数 | 64 | 压力测试 |
| JSON 序列化耗时 | < 10μs | 单次调用计时 |

### NFR-02：可靠性

| 指标 | 目标值 |
|------|--------|
| 编译警告 | 0（-Wall -Wextra -Werror） |
| 内存泄漏 | 0（Valgrind 验证） |
| 单客户端断开不影寳到其他客户端 | 是 |
| 配置文件缺失时使用默认值 | 是 |

### NFR-03：可移植性

| 目标 | 说明 |
|------|------|
| 目标架构 | x86_64 / ARM64（香橙派 Zero 3，Cortex-A53） |
| 编译器 | gcc ≥ 9.0，C11 标准 |
| 内核模块 | Linux ≥ 5.10 |
| 依赖 | **零第三方库**，仅 libc + libpthread |

### NFR-04：可维护性

| 要求 | 说明 |
|------|------|
| 代码规范 | 自描述变量名、中文注释、一个函数 < 60 行 |
| 文档 | DESIGN.md（架构）+ README.md（部署）+ 代码内注释 |
| 构建 | Makefile 一键编译，make clean 清理所有产物 |

---

## 4. 使用场景

### 场景 1：手机远程监控家庭环境

```
用户打开手机 App ──→ TCP 连接网关:8888
            ──→ JSON 数据流实时推送
            ──→ 温度过高时 App 弹窗提醒
            ──→ 关闭 App ──→ 网关 30s 超时回收连接
```

### 场景 2：开发人员 nc 调试

```bash
nc 192.168.1.100 8888
# 连上后立即看到全量快照：
{"result":"LIST","data":[{"event":"temp",...},{...}]}
# 实时数据自动推送：
{"event":"temp","value":26.3,"unit":"°C","time":1753861200}
# 只订阅温度：
{"cmd":"SUB","type":["temp"]}
# 之后只收到 temp，其他类型被过滤
```

### 场景 3：运维监控

```
运维客户端连接 → 订阅 sysinfo
               → 每 5 秒收到 CPU/内存/进程数
               → 写脚本记录到 Prometheus / InfluxDB
```

---

## 5. 约束条件

| 约束 | 说明 |
|------|------|
| C1 | **禁止引入第三方 JSON 库**（cJSON/jansson 等）— 体现协议设计能力 |
| C2 | **禁止在广播遍历时加长锁** — 大锁只能在入/出客户端列表时短暂持有 |
| C3 | **内核模块代码独立存放**，用 `#ifdef KERNEL_MODULE` 与用户态代码分隔 |
| C4 | **所有 Epoll fd 必须设 O_NONBLOCK** — 事件循环内不能有阻塞调用 |
| C5 | **日志不写磁盘** — printf 到 stdout，由 systemd/journald 收集 |

---

## 6. 与 v1.0 的差异对照

| 维度 | v1.0 | v2.0 |
|------|------|------|
| 协议格式 | 纯文本 + emoji | JSON 结构化 |
| 推送策略 | 全量广播 | 每客户端按位掩码过滤 |
| 命令支持 | 1 个（LIST） | 4 个（SUB/PING/LIST/CLIENTS） |
| 断线检测 | 被动（write 返回 0 才发现） | 主动（30s 心跳超时） |
| 系统监控 | 无 | /proc + netlink 采集 CPU/内存 |
| 内核交互 | 无 | 自定义内核模块 /proc/gateway/info |
| 代码量 | ~820 行 | ~2000 行 |
| 第三方库 | 0 | 0（保持零依赖） |
| 端口配置 | 硬编码 8888 | 配置文件驱动 |
| 编译环境 | x86_64 only | x86_64 + ARM64 交叉编译 |

---

## 7. 验收标准

### 7.1 功能验收

| 编号 | 验收项 | 判定标准 |
|------|--------|---------|
| AC-01 | JSON 推送 | `nc` 连接后看到的每一行为合法 JSON |
| AC-02 | 订阅过滤 | 发 `{"cmd":"SUB","type":["temp"]}` 后只收到 temp 事件 |
| AC-03 | 心跳 PONG | 发 `{"cmd":"PING"}` 后收到 `{"result":"PONG"}` |
| AC-04 | 心跳超时 | 30s 不发 PING，服务器日志显示 client disconnected |
| AC-05 | 全量快照 | 发 `{"cmd":"LIST"}` 后收到所有传感器最新值 |
| AC-06 | 错误处理 | 发 `{"cmd":"FOO"}` 后收到 `{"result":"ERR","reason":"..."}` |
| AC-07 | 系统信息 | 5s 内收到至少一条 `{"event":"sysinfo",...}` |
| AC-08 | 配置文件缺失 | 不传 gateway.conf 时使用默认值，程序正常运行 |

### 7.2 质量验收

| 编号 | 验收项 | 判定标准 |
|------|--------|---------|
| AC-09 | 编译 | `make` 零警告（-Wall -Wextra -Werror） |
| AC-10 | 内存 | `valgrind --leak-check=full` 无泄漏 |
| AC-11 | 交叉编译 | `make CC=aarch64-linux-gnu-gcc` 通过，scp 到香橙派可运行 |
| AC-12 | 内核模块 | `insmod gw_info.ko` 后 `cat /proc/gateway/info` 有输出 |

### 7.3 文档验收

| 编号 | 验收项 |
|------|--------|
| AC-13 | DESIGN.md 包含架构图、数据流图、设计决策记录 |
| AC-14 | README.md 包含构建步骤、部署方法、API 协议说明 |
| AC-15 | 代码注释覆盖所有公开 API 和关键逻辑段 |

---

## 8. 里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| M1: JSON 协议 + 订阅 + 心跳 | 7/31 | json_proto.c/h + server.c 重写 |
| M2: 系统监控（/proc + netlink） | 8/1 | netlink_monitor.c/h |
| M3: 内核模块 | 8/2 | gw_info/hello.c + Makefile |
| M4: 整合 + 文档 | 8/3 | README.md + 端到端测试 |
| M5: 交叉编译 + 实机验证 | 8/4 | 香橙派 Zero 3 运行 |

---

*文档版本：v1.0 | 最后更新：2026-07-30*
